export { isBrowserExtension, migrateSyncToLocal } from "./runtime"
export type {
  BookmarkNode,
  BookmarkDiagnostic,
  BookmarkAdapter,
  StorageAdapter,
  FaviconProvider,
  AdapterCapabilities,
  BrowserAdapter,
  AdapterHealth,
} from "./types"
