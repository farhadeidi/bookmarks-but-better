# bun owns dependencies and the lockfile (bun.lock). npm is used for exactly one
# thing here — publishing packages/bookmarks-but-better — and must not leave a
# package-lock.json behind when it does.
package-lock=false
